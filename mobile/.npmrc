# EAS Build runs "npm ci --include=dev" which fails on optional peer-dep
# conflicts (react-dom@19.2.6 wants react@^19.2.6 but Expo SDK 54 pins
# react@19.1.0).  Legacy mode keeps backward-compatible resolution behaviour.
legacy-peer-deps=true
